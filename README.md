# hOS Server releases

Update feed and signed builds for hOS Server (household test cycle).
Source lives in the private `hos` repository.

- `appcast.xml` — Sparkle update feed consumed by the app
- Builds are attached to GitHub Releases on this repo

Install instructions ship inside each zip (INSTALL.md).
